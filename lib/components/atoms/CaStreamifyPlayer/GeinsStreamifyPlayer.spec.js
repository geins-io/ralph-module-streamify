import { shallowMount } from '@vue/test-utils';
import GeinsStreamifyPlayer from './GeinsStreamifyPlayer.vue';

describe('GeinsStreamifyPlayer.vue', () => {
  it('renders a component', () => {
    const component = shallowMount(GeinsStreamifyPlayer);
  });
});
