import { shallowMount } from '@vue/test-utils';
import GeinsWidgetStreamify from './GeinsWidgetStreamify.vue';

describe('GeinsWidgetStreamify.vue', () => {
  it('renders a component', () => {
    const component = shallowMount(GeinsWidgetStreamify);
  });
});
